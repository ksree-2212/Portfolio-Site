MONGODB_URI=your_mongodb_atlas_connection_string
JWT_SECRET=generate_a_long_random_string_here
ALLOWED_ORIGINS=https://your-portfolio-domain.vercel.app
PORT=5000
# Optional — a GitHub personal access token (no scopes needed for public repos)
# raises the GitHub API rate limit from 60/hour to 5000/hour for the auto-import feature.
GITHUB_TOKEN=
